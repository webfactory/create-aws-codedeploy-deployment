{
  "version": "1.0.0",
  "scripts": {
    "build": "ncc build src/create-deployment.cjs -o dist/create-deployment; ncc build src/delete-deployment-group.cjs -o dist/delete-deployment-group"
  },
  "dependencies": {
    "@actions/core": "^3.0",
    "@actions/github": "^9.0",
    "@aws-sdk/client-codedeploy": "^3.629.0",
    "js-yaml": "^4.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44"
  }
}
