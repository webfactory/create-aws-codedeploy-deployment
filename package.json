{
  "main": "index.js",
  "version": "0.1.0",
  "scripts": {
    "build": "ncc build src/create-deployment.js -o dist/create-deployment; ncc build src/delete-deployment-group.js -o dist/delete-deployment-group"
  },
  "dependencies": {
    "@actions/core": "^1.10",
    "@actions/github": "^6.0",
    "@aws-sdk/client-codedeploy": "^3.629.0",
    "js-yaml": "^4.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38"
  }
}
